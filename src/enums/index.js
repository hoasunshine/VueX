export const SizeButton = Object.freeze({ default: 'default', full: 'full'});
export const TypeButton = Object.freeze({
  primary: 'primary',
  success: 'success',
  danger: 'danger'
});
export const OptionValues = Object.freeze({ low: 0, normal: 1, high: 2 });